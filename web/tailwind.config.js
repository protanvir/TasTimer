/** @type {import('tailwindcss').Config} */
export default {
  // Tailwind v4: source scanning is automatic (Oxide engine).
  // The content array from v3 is not used — all .ts/.tsx files under
  // src/ are included automatically. Use `@source` in CSS for extras.
  theme: {
    extend: {},
  },
  plugins: [],
}
